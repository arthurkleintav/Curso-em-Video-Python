sal = float(input('Salário do funcionário: R$'))
aum = sal + (sal * 15 / 100)
print('Com o aumento de 15%, o salário do funcionário irá aumentar de R${:.2f} para R${:.2f}'.format(sal, aum))

# sal = float(input('Salário do funcionário: R$'))
# aum = sal + (sal * 0.15)
# print('Com o aumento de 15%, o salário do funcionário irá aumentar de R${:.2f} para R${:.2f}'.format(sal, aum))

# sal = float(input('Salário do funcionário: R$'))
# aum = sal * 1.15
# print('Com o aumento de 15%, o salário do funcionário irá de R${:.2f} para R${:.2f}'.format(sal, aum))