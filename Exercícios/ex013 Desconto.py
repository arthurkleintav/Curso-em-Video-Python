# val = float(input('Digite um valor: R$'))
# desc = val - (val * 5 / 100)
# print('Com 5% de desconto, o valor do produto vai de {:.2f} para {:.2f}.'.format(val, desc))

val = float(input('Digite um valor: R$'))
desc = val - (val * 0.05)
print('Com 5% de desconto, o valor do produto vai de {:.2f} para {:.2f}.'.format(val, desc))

# val = float(input('Digite um valor: R$'))
# desc = val * 0.95
# print('Com 5% de desconto, o valor do produto vai de {:.2f} para {:.2f}.'.format(val, desc))

